export interface Config {
  terminologyUrl: string;
  codeListUrl: string;
  dataModelUrl: string;
  commentsUrl: string;
  dev: boolean;
  env: string;
  messagingEnabled: boolean;
  fakeLoginAllowed: boolean;
}
